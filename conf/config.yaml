defaults:
  - model: transformer
  - data: fields
  - trainer: default
  - _self_

hydra:
  run:
    dir: ${paths.results}/outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}

# --- Global Project Configuration ---
project_name: "fields-transformer"
mode: train
mlflow_run_id: null
version: "1.0.0"
matmul_precision: "medium"

# --- Single Source of Truth: paths ---
paths:
  # absolute path for project root
  project_root: ${hydra:runtime.cwd}
  # root output directory for all runs
  results: "${paths.project_root}/results/transformer"
  nf_data: "${paths.project_root}/data/dataset_155.pt"
  ir_data: "${paths.project_root}/data/IR_v09_df_all_features.parquet"
  ckpt: null #"${paths.results}/mlruns/168466128006132285/93ef1d69016445afbb2bb3d813f38787/artifacts/checkpoints/best-model.ckpt"
